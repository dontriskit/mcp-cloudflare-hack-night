{
  "name": "whitecontext-mcp",
  "version": "1.0.0",
  "description": "MCP server for WhiteContext API - Company intelligence analysis",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.9",
    "mcp-lite": "^0.3.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "@cloudflare/workers-types": "^4.20241127.0"
  }
}
